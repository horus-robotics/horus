#include <stdio.h>
#include <math.h>

int main() {
    double result = sqrt(16.0);
    printf("sqrt(16) = %.1f\n", result);

    result = pow(2.0, 3.0);
    printf("pow(2,3) = %.1f\n", result);

    return 0;
}
