# Robot configuration for sim2d (TOML format)
# Simple 2D robot configuration matching the current implementation

width = 0.5        # Robot width in meters
length = 0.8       # Robot length in meters
max_speed = 2.0    # Maximum velocity in m/s
color = [0.2, 0.8, 0.2]  # RGB color [0.0-1.0] - Green

# Example variations:
# Blue robot:
#   color = [0.2, 0.2, 0.8]
# Red robot:
#   color = [0.8, 0.2, 0.2]
# Larger robot:
#   width = 0.7
#   length = 1.2
