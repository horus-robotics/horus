---
title: Dashboard Guide
description: Monitor, debug, and manage your HORUS applications in real-time
order: 5
---

# Dashboard Guide

The HORUS Dashboard is your mission control for robotics applications - monitor performance, debug issues, and tune parameters in real-time from any device on your network.

## Quick Start

### Launch the Dashboard

```bash
# Web dashboard (opens in browser)
horus dashboard

# Custom port
horus dashboard 8080

# Terminal UI mode
horus dashboard --tui
```

**Access from anywhere:**
```
• Local device:    http://localhost:3000
• Other devices:   http://192.168.1.100:3000
```

The dashboard automatically binds to all network interfaces, so you can monitor your robot from your phone, tablet, or another computer.

## What You'll See

### System Overview

At the top of the dashboard, you'll see:

**System Health Badge**
- 🟢 **Healthy** - All nodes running normally
- 🟡 **Warning** - Some nodes reporting warnings
- 🟠 **Degraded** - Errors detected in nodes
- 🔴 **Critical** - Critical failures
- ⚪ **Idle** - No nodes running

**Quick Stats**
- Active nodes count
- Active topics count
- HORUS version

### Main Sections

## 1. Nodes Tab

**What it shows:** All running HORUS nodes with live performance metrics

**You'll see:**

| Node Name | Status | Health | CPU | Memory | Tick Rate | Errors |
|-----------|--------|--------|-----|--------|-----------|--------|
| velocity_controller | Running | 🟢 Healthy | 2.3% | 45 MB | 60.2 Hz | 0 |
| lidar_processor | Running | 🟡 Warning | 15.1% | 128 MB | 59.8 Hz | 3 |
| motor_driver | Running | 🟢 Healthy | 1.8% | 32 MB | 100.1 Hz | 0 |

**What to watch:**
- **Tick Rate** - Should match your target (default 60 Hz)
  - ✅ Good: 59-61 Hz
  - ⚠️ Warning: 50-59 Hz or 61-70 Hz
  - ❌ Problem: <50 Hz (system overloaded)

- **CPU Usage** - Shows computation load
  - ✅ Good: <30%
  - ⚠️ Monitor: 30-80%
  - ❌ Problem: >80% sustained

- **Memory** - RSS memory usage
  - ⚠️ Watch for continuous growth (possible leak)

- **Error Count** - Cumulative errors
  - ✅ Good: 0 or slow growth
  - ❌ Problem: Rapidly increasing

**Use it to:**
- Identify performance bottlenecks
- Spot nodes that are struggling
- Verify all expected nodes are running

## 2. Topics Tab

**What it shows:** All active communication topics and their memory usage

**You'll see:**

| Topic | Size | Active | Subscribers |
|-------|------|--------|-------------|
| sensors/lidar | 128 KB | 🟢 | 3 |
| motors/cmd_vel | 8 KB | 🟢 | 2 |
| vision/camera | 1.2 MB | 🟢 | 1 |

Topics are automatically formatted:
- Internal: `horus_sensors_lidar`
- Display: `sensors/lidar` (easier to read)

**Use it to:**
- Verify topics are created
- Check subscriber counts (is anyone listening?)
- Monitor memory usage in `/dev/shm`
- Debug communication issues

## 3. Graph View

**What it shows:** Visual network of how nodes connect through topics

**You'll see:**
- 🔵 **Blue circles** - Your HORUS nodes (processes)
- 🟢 **Green circles** - Topics (communication channels)
- ➡️ **Arrows** - Publish/subscribe relationships
  - Node → Topic = Publishing
  - Topic → Node = Subscribing

**Interactive features:**
- **Click nodes** - Highlight connections
- **Drag nodes** - Rearrange layout
- **Zoom** - Mouse wheel or pinch
- **Pan** - Click and drag background

**Use it to:**
- Visualize system architecture
- Debug: "Why isn't my node receiving messages?"
- Verify pub/sub connections are correct
- Understand data flow

## 4. Logs View

**What it shows:** Real-time logs from all nodes and topics

**Filter options:**
- **All Logs** - System-wide feed
- **By Node** - Select specific node from dropdown
- **By Topic** - Filter by topic name
- **By Level** - Info, Debug, Warning, Error

**Log entry format:**
```
[2025-10-24 12:34:56.789] [velocity_controller] [INFO] Published velocity: 1.5 m/s
  ↳ IPC: 296ns | Tick: 12μs
```

**Performance metrics shown:**
- **IPC time** - Inter-process communication overhead
- **Tick time** - Total execution time for this tick

**Use it to:**
- Debug runtime issues
- Monitor message flow
- Check IPC performance
- Track down errors

## 5. Parameters

**What it shows:** Live parameter editor with instant updates

**You'll see:**

| Parameter | Value | Type | Actions |
|-----------|-------|------|---------|
| motion.max_speed | 1.5 | number | ✏️ Edit  🗑️ Delete |
| safety.emergency_stop | false | boolean | ✏️ Edit  🗑️ Delete |
| control.pid.kp | 1.0 | number | ✏️ Edit  🗑️ Delete |

**Features:**
- **Live editing** - Changes apply immediately
- **Auto-save** - Saves to `~/.horus/config/params.yaml`
- **Add new** - Create parameters on the fly
- **Export** - Save as YAML for backup
- **Import** - Load from YAML/JSON file

**Common use case - Tuning a PID controller:**
1. Click ✏️ next to `control.pid.kp`
2. Enter new value: `1.5`
3. Hit Enter
4. Watch robot behavior change immediately
5. Adjust `ki` and `kd` the same way
6. When satisfied, click "Export" to save configuration

**Parameter organization:**
```yaml
motion:
  max_speed: 1.5
  acceleration: 0.5

safety:
  emergency_stop: false
  max_temperature: 80.0

control:
  pid:
    kp: 1.0
    ki: 0.1
    kd: 0.01
```

## 6. Packages

**What it shows:** Package browser and installer

**Features:**
- **Search Registry** - Find packages by name/description
- **Browse Environments** - Pre-configured setups
- **Install** - One-click package installation
- **Publish** - Upload your packages (requires auth)

**Example workflow:**
1. Search for "lidar"
2. See results: `velodyne_driver`, `rplidar_ros2`, etc.
3. Click "Install" next to desired package
4. Choose: Install globally or locally
5. Package downloads and installs automatically

## 7. Remote Deployment

**What it shows:** Deploy and manage code on physical robots

**Features:**
- **Deploy** - Send code to robot over network
- **Monitor** - Check CPU, memory, temperature
- **View Logs** - See robot's stdout/stderr
- **Stop** - Kill running deployments

**Deploy workflow:**
1. Enter robot ID: `robot_lab_01`
2. Select file: `main.rs`
3. Choose: Debug or Release build
4. Click "Deploy"
5. Dashboard shows build progress
6. View robot hardware stats and logs

**Hardware monitoring:**
```
CPU: 45%
Memory: 2.1 GB / 4 GB
Disk: 15.2 GB free
Temperature: 52°C
```

## Common Workflows

### Debugging: Node Not Receiving Messages

**Problem:** Your subscriber node isn't getting data

**Dashboard investigation:**
1. **Nodes Tab** → Verify both publisher and subscriber are listed
2. **Topics Tab** → Check topic exists and shows >0 subscribers
3. **Graph View** → Verify visual connection exists
4. **Logs → Filter by node** → Check for connection errors

**Common findings:**
- Topic name mismatch (typo in pub/sub)
- Publisher not running
- Shared memory permissions issue
- Namespace confusion

### Performance Optimization

**Problem:** System running slow

**Dashboard investigation:**
1. **Nodes Tab** → Sort by CPU (click column header)
2. Identify high CPU nodes
3. Check their tick rates - are they hitting target?
4. **Logs** → Filter by that node → Look for "slow tick" warnings

**Red flags:**
- Tick rate <50 Hz on 60 Hz node
- CPU >80% sustained
- Memory continuously growing
- Errors increasing rapidly

### Live Parameter Tuning

**Scenario:** Tuning robot motion parameters

**Dashboard workflow:**
1. **Parameters Tab**
2. Find `motion.max_speed`, `motion.acceleration`
3. Edit values while watching robot
4. Check **Nodes Tab** → `motion_controller` → CPU/tick rate
5. Adjust until behavior is optimal
6. Click "Export Parameters" to save
7. Copy YAML to your config file

## Tips & Best Practices

### Performance Monitoring

**What to watch:**
- Tick rate stability (should be consistent)
- CPU usage trends (spikes are OK, sustained high is bad)
- Memory growth (flat is good, growing is concerning)
- Error count (should be 0 or very slow growth)

**When to investigate:**
- Tick rate drops below 90% of target
- CPU >80% for more than a few seconds
- Memory growing >10 MB/minute
- Error count increasing >1/second

### Parameter Management

**Best practices:**
- Use hierarchical structure: `category.subcategory.param`
- Lowercase with underscores: `max_speed` not `MaxSpeed`
- Descriptive names: `motion.max_linear_velocity` not `motion.vel`
- Export parameters regularly for backup
- Version control your `params.yaml`

### Network Access

**Multi-device monitoring:**

✅ **Works great for:**
- Monitoring robot from your desk
- Team members viewing same dashboard
- Tablet/phone for mobile monitoring
- Remote debugging over VPN

⚠️ **Security note:**
- No authentication by default
- Only use on trusted networks
- For production: Use localhost only or add auth layer
- Consider firewall rules for external access

**Firewall setup:**
```bash
# Allow dashboard access on port 3000
sudo ufw allow 3000/tcp
```

## Troubleshooting

### Dashboard shows "No nodes running"

**Cause:** No HORUS application is currently running

**Solution:**
```bash
# Terminal 1 - Start your app
horus run main.rs

# Terminal 2 - Start dashboard
horus dashboard
```

### Logs are empty

**Cause:** Shared memory log buffer doesn't exist

**Solution:**
1. Run a HORUS app first (creates `/dev/shm/horus_logs`)
2. Then start dashboard
3. Refresh browser if needed

### Can't access from phone/tablet

**Cause:** Firewall blocking or wrong IP

**Solution:**
```bash
# 1. Check your computer's IP
ip addr show | grep inet

# 2. Allow port in firewall
sudo ufw allow 3000/tcp

# 3. Access from device
# Use: http://192.168.1.100:3000 (your actual IP)
```

### Port already in use

**Error:** `Port 3000 is already in use`

**Solution:**
```bash
# Use different port
horus dashboard 3001

# Or kill existing process
lsof -ti:3000 | xargs kill -9
```

### Parameters not saving

**Cause:** Config directory doesn't exist or lacks permissions

**Solution:**
```bash
# Create config directory
mkdir -p ~/.horus/config

# Fix permissions
chmod 755 ~/.horus/config
```

## Terminal UI Mode

For headless servers or terminal purists:

```bash
horus dashboard --tui
```

**Features:**
- All monitoring capabilities
- Keyboard navigation
- ASCII visualizations
- Perfect for SSH sessions

**Controls:**
- `↑↓` - Navigate
- `Tab` - Switch sections
- `Enter` - Select/expand
- `q` - Quit

## API Reference

For developers building custom integrations, the dashboard provides a REST API:

**Base:** `http://localhost:3000/api`

**Key endpoints:**
- `GET /status` - System health
- `GET /nodes` - All nodes with metrics
- `GET /topics` - Active topics
- `GET /graph` - Node-topic relationships
- `GET /logs/all` - System logs
- `GET /logs/node/:name` - Node-specific logs
- `GET /params` - All parameters
- `POST /params/:key` - Set parameter
- `GET /ws` - WebSocket for real-time updates

**Full API documentation:** See [API Reference](#) for complete endpoint specs

## Next Steps

- **[CLI Reference](/cli-reference)** - All dashboard commands
- **[Parameters Guide](/parameters)** - Deep dive into param system
- **[Performance Guide](/performance)** - Optimization techniques
- **[Examples](/examples)** - Real-world usage patterns
